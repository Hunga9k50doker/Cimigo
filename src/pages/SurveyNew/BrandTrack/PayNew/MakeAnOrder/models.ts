export interface CustomSlide {
    navigator: boolean;
    dots: boolean;
    infinite: boolean;
    speed: number;
    slidesToShow: number;
    slidesToScroll: number;
    prevArrow: React.ReactNode;
    nextArrow: React.ReactNode;
}
